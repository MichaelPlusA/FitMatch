@{
    Layout = "~/Views/Shared/_LayoutTrainer.cshtml";
}

@using Capstone.Web.Models
@using Capstone.Web.Models.ViewModel
@model PopulatePlanViewModel

<h1>hi</h1>

@foreach (Workout workout in Model.Workouts)
{
    <h1>@workout.Name</h1>
    <input type="hidden" value=@workout.Id id="workoutID" />
    <table name="exerciseTable" id="exerciseTable"></table>
    <h1>Add Strength Exercise</h1>
    <select>
        @foreach (Exercise exercise in Model.Exercises)
        {
            if (exercise.Type == 2)
            {
                <option value=@exercise.ExerciseID id="exercises" selected="selected">@exercise.Name</option>
            }
        }
    </select>
    <input type="number" placeholder="sets" id="sets" />
    <input type="number" placeholder="reps" id="reps" />
    <button name="exerciseButton" id="exerciseButton">Add Exercise</button>
    <h1>Add Cardio Exercises</h1>
    <select>
        @foreach (Exercise exercise in Model.Exercises)
        {
            if (exercise.Type == 1)
            {
                <option value=@exercise.ExerciseID id="exercises" selected="selected">@exercise.Name</option>
            }
        }
    </select>
    <input type="number" placeholder="duration" id="Duration" />
    <input type="number" placeholder="intensity(1-10)" id="Intensity" />
    <button name="exerciseButton" id="exerciseButton">Add Exercise</button>
}